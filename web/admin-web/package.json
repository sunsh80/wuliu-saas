{
  "name": "admin-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173 --strict-port",
    "dev:proxy": "vite --port 5173 --strict-port",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .js,.vue",
    "format": "prettier --write .",
    "serve": "vite --host --port 5173",
    "db:test": "node test-db-connection.js",
    "db:connect": "node db-connector.js",
    "verify": "echo \"请访问 http://localhost:5173/connection-verification.html 进行连接验证\""
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.5",
    "mysql2": "^3.6.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "eslint": "^8.56.0",
    "prettier": "^3.1.0"
  }
}